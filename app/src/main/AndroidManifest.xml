<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="arqdesis.usjt.br.projetocaixaeletronico">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Controller.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Controller.ListaExtratoResultadoActivity" />
        <activity android:name=".Controller.ListaExtratoActivity" />
        <activity android:name=".Controller.SaqueActivity" />
        <activity android:name=".Controller.OutrosValoresActivity" />
        <activity android:name=".Controller.DebitoAutomaticoActivity" />
        <activity android:name=".Controller.TransferenciaActivity"></activity>
    </application>

</manifest>